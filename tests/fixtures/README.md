# Test Fixtures

Deterministic folder pairs + sample files for unit, integration, and E2E
tests. Each subfolder includes a README describing the expected diff
classification so tests can assert against a known map.

Populated from Phase 4 / Phase 12 onward.
